[package]
name = "pgsafe"
version = "0.0.1"
edition = "2021"

[dependencies]
pg_query = "6.1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
clap = { version = "4", features = ["derive"] }

[dev-dependencies]
assert_cmd = "2.2"
predicates = "3.1"
