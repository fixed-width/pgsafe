//! Connection handshake, document store, and the dispatch loop.

use super::LspError;

/// Placeholder serve loop; implemented in the server task.
pub(crate) fn serve() -> Result<(), LspError> {
    Ok(())
}
