---
import Base from "../layouts/Base.astro";
import Nav from "../components/Nav.astro";
import Footer from "../components/Footer.astro";
const title = "pgsafe playground — lint PostgreSQL migrations in your browser";
const description =
  "Paste a PostgreSQL migration and see pgsafe's findings instantly — the real linter, running in your browser via WebAssembly.";
---
<Base title={title} description={description}>
  <Nav />
  <main class="container pg">
    <h1>Playground</h1>
    <p class="lede">The real pgsafe analyzer, compiled to WebAssembly and running in your browser. Nothing is sent anywhere.</p>
    <div class="toolbar">
      <label class="field"><input type="checkbox" id="opt-intx" /> Treat as in a transaction</label>
      <label class="field">Fail on
        <select id="opt-failon">
          <option value="warning">warning</option>
          <option value="error">error</option>
          <option value="never">never</option>
        </select>
      </label>
      <select id="examples" aria-label="Load an example">
        <option value="">Load an example…</option>
      </select>
      <button id="permalink" type="button">Copy link</button>
    </div>
    <div class="grid">
      <div id="editor" aria-label="SQL editor"></div>
      <section id="results" aria-live="polite"><p class="status" id="status">Loading the linter…</p></section>
    </div>
  </main>
  <Footer />
</Base>
<script>
  import "../playground/main.ts";
</script>
<style>
  .pg { padding-top: 2rem; }
  .lede { color: var(--fg-dim); max-width: 46rem; }
  .toolbar { display: flex; flex-wrap: wrap; gap: 1rem; align-items: center; margin: 1.25rem 0; color: var(--fg-dim); }
  .toolbar .field { display: inline-flex; align-items: center; gap: 0.5rem; }
  .toolbar select, .toolbar button { background: var(--bg-elev); color: var(--fg); border: 1px solid rgba(255, 255, 255, 0.16); border-radius: 6px; padding: 0.35rem 0.6rem; font-family: var(--mono); }
  .toolbar button { cursor: pointer; }
  .toolbar button:hover { border-color: var(--accent); }
  .grid { display: grid; grid-template-columns: 1fr 1fr; gap: 1.25rem; align-items: start; }
  #editor { border: 1px solid var(--border); border-radius: 10px; overflow: hidden; height: 28rem; }
  #editor :global(.cm-editor) { height: 100%; }
  #editor :global(.cm-editor.cm-focused) { outline: none; }
  #results { border: 1px solid var(--border); border-radius: 10px; background: var(--bg-elev); padding: 1rem; height: 28rem; overflow-y: auto; font-size: 0.9rem; }
  /* Result rows are built in JS, so they lack Astro's scope attribute — target
     them as :global under the static #results container. */
  #results :global(.status) { color: var(--fg-dim); }
  #results :global(.clean) { color: var(--safe); }
  #results :global(.finding) { padding: 0.7rem 0.5rem; margin: 0 -0.5rem; border-top: 1px solid color-mix(in srgb, var(--border) 70%, transparent); border-radius: 6px; }
  #results :global(.finding:hover) { background: rgba(255, 159, 28, 0.07); border-top-color: transparent; }
  #results :global(.finding:first-child) { border-top: 0; padding-top: 0; }
  #results :global(.finding .head) { display: flex; gap: 0.5rem; align-items: baseline; flex-wrap: wrap; }
  #results :global(.finding a) { color: var(--accent); }
  #results :global(.finding.suppressed) { opacity: 0.55; }
  #results :global(.finding .ignored) { font-size: 0.66rem; text-transform: uppercase; letter-spacing: 0.05em; color: var(--fg-dim); border: 1px solid var(--border); border-radius: 4px; padding: 0.02rem 0.32rem; }
  #results :global(.finding .msg) { color: var(--fg); margin: 0.25rem 0; }
  #results :global(.finding .msg code) { background: var(--bg); border: 1px solid rgba(255, 255, 255, 0.16); border-radius: 4px; padding: 0 0.25rem; font-size: 0.9em; }
  #results :global(.finding .loc) { color: var(--fg-dim); font-size: 0.8rem; margin: 0; }
  #results :global(.finding .actions) { display: flex; gap: 0.4rem; margin-top: 0.5rem; }
  #results :global(.finding .actions button) { font-family: var(--mono); font-size: 0.75rem; background: var(--bg-elev); color: var(--fg); border: 1px solid var(--border); border-radius: 5px; padding: 0.15rem 0.55rem; cursor: pointer; }
  #results :global(.finding .actions button:hover) { border-color: var(--accent); }
  #results :global(.finding .actions button.fix) { color: var(--accent); border-color: rgba(255, 159, 28, 0.45); }
  #results :global(.finding .actions button.fix:hover) { background: rgba(255, 159, 28, 0.1); }
  #results :global(.gate) { margin-top: 0.75rem; color: var(--fg-dim); padding-top: 0.5rem; border-top: 1px solid var(--border); }
  @media (max-width: 820px) { .grid { grid-template-columns: 1fr; } }
</style>
